name: improved_wrap_example
description: Example app for animated_containers package
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  improved_wrap:
    path: ../
  cupertino_icons: ^1.0.2
  circular_reveal_animation: ^2.0.1
  flutter_touch_ripple: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true 